---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const contactInfo = [
  {
    icon: '📞',
    title: 'Phone',
    value: '+91-8305020077',
    link: 'tel:+918305020077'
  },
  {
    icon: '📧',
    title: 'Email',
    value: 'contact@drag-ai.com',
    link: 'mailto:contact@drag-ai.com'
  },
  {
    icon: '📍',
    title: 'Location',
    value: 'ULF, Bellandur, Bengaluru',
    link: 'https://maps.google.com/?q=Bellandur,Bengaluru'
  }
];

const services = [
  'AI Strategy & Consulting',
  'Custom AI Development',
  'AI Integration & Implementation',
  'Training & Change Management',
  'Ongoing Support & Optimization'
];
---

<Layout 
  title="Contact Drag-AI | Get Your Free AI Assessment"
  description="Ready to transform your business with AI? Contact Drag-AI for a free consultation and AI readiness assessment."
>
  <Header />

  <!-- Hero Section -->
  <section class="hero-gradient text-white py-16">
    <div class="container relative z-10">
      <div class="max-w-3xl mx-auto text-center">
        <h1 class="text-4xl md:text-5xl font-bold mb-6">
          Let's Transform Your Business <span class="gradient-text">Together</span>
        </h1>
        <p class="text-xl text-gray-300">
          Get a complimentary AI readiness assessment and discover how AI can improve your operations.
        </p>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="section">
    <div class="container">
      <div class="grid lg:grid-cols-2 gap-12">
        <!-- Contact Form -->
        <div>
          <h2 class="text-3xl font-bold mb-6">Get Your Free AI Assessment</h2>
          <p class="text-gray-300 mb-8">
            Fill out the form below and we'll get back to you within 24 hours to schedule your free consultation.
          </p>

          <form
            id="contact-form"
            action="https://api.web3forms.com/submit"
            method="POST"
            class="space-y-6"
          >
            <!-- Web3Forms Access Key - Replace with your actual key -->
            <input type="hidden" name="access_key" value="af781fa2-7ab3-4154-8aee-f123f4ae54c5">
            <input type="hidden" name="subject" value="New Contact Form Submission from Drag-AI Website">
            <input type="hidden" name="from_name" value="Drag-AI Website">
            
            <!-- Honeypot for spam protection -->
            <input type="checkbox" name="botcheck" class="hidden" style="display: none;">

            <!-- Name -->
            <div>
              <label for="name" class="block text-sm font-semibold text-gray-300 mb-2">
                Full Name *
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 bg-slate-800 border border-cyan-500/30 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all text-white placeholder-gray-500"
                placeholder="John Doe"
              />
            </div>

            <!-- Email -->
            <div>
              <label for="email" class="block text-sm font-semibold text-gray-300 mb-2">
                Email Address *
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 bg-slate-800 border border-cyan-500/30 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all text-white placeholder-gray-500"
                placeholder="john@company.com"
              />
            </div>

            <!-- Phone -->
            <div>
              <label for="phone" class="block text-sm font-semibold text-gray-300 mb-2">
                Phone Number
              </label>
              <input
                type="tel"
                id="phone"
                name="phone"
                class="w-full px-4 py-3 bg-slate-800 border border-cyan-500/30 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all text-white placeholder-gray-500"
                placeholder="+91-1234567890"
              />
            </div>

            <!-- Company -->
            <div>
              <label for="company" class="block text-sm font-semibold text-gray-300 mb-2">
                Company Name *
              </label>
              <input
                type="text"
                id="company"
                name="company"
                required
                class="w-full px-4 py-3 bg-slate-800 border border-cyan-500/30 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all text-white placeholder-gray-500"
                placeholder="Your Company"
              />
            </div>

            <!-- Industry -->
            <div>
              <label for="industry" class="block text-sm font-semibold text-gray-300 mb-2">
                Industry *
              </label>
              <select
                id="industry"
                name="industry"
                required
                class="w-full px-4 py-3 bg-slate-800 border border-cyan-500/30 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all text-white"
              >
                <option value="">Select your industry</option>
                <option value="construction">Construction & Specialty Trades</option>
                <option value="hospitality">Hospitality (Restaurants & Hotels)</option>
                <option value="logistics">Transportation & Logistics</option>
                <option value="property">Property Management & Real Estate</option>
                <option value="insurance">Insurance Brokers & Agencies</option>
                <option value="professional">Professional Services (Legal, Accounting, HR)</option>
                <option value="retail">Multi-Store Retail & E-Commerce</option>
                <option value="other">Other</option>
              </select>
            </div>

            <!-- Service Interest -->
            <div>
              <label class="block text-sm font-semibold text-gray-300 mb-2">
                What are you interested in? *
              </label>
              <div class="space-y-2">
                {services.map((service) => (
                  <label class="flex items-center">
                    <input
                      type="checkbox"
                      name="services[]"
                      value={service}
                      class="w-4 h-4 text-cyan-500 bg-slate-800 border-cyan-500/30 rounded focus:ring-cyan-500"
                    />
                    <span class="ml-2 text-gray-300">{service}</span>
                  </label>
                ))}
              </div>
            </div>

            <!-- Message -->
            <div>
              <label for="message" class="block text-sm font-semibold text-gray-300 mb-2">
                Tell us about your needs *
              </label>
              <textarea
                id="message"
                name="message"
                required
                rows="5"
                class="w-full px-4 py-3 bg-slate-800 border border-cyan-500/30 rounded-lg focus:ring-2 focus:ring-cyan-500 focus:border-transparent transition-all resize-none text-white placeholder-gray-500"
                placeholder="Describe your current challenges and what you hope to achieve with AI..."
              ></textarea>
            </div>

            <!-- Submit Button -->
            <div>
              <button
                type="submit"
                class="w-full btn btn-primary text-lg py-4"
              >
                Get Free Assessment
              </button>
              <p class="text-sm text-gray-400 mt-3 text-center">
                We'll respond within 24 hours • No commitment required
              </p>
            </div>
          </form>

          <!-- Success/Error Messages -->
          <div id="form-success" class="hidden mt-6 p-4 bg-green-900/50 border border-green-500/50 rounded-lg">
            <p class="text-green-300 font-semibold">✓ Thank you for your message!</p>
            <p class="text-green-400 text-sm mt-1">We'll get back to you within 24 hours.</p>
          </div>

          <div id="form-error" class="hidden mt-6 p-4 bg-red-900/50 border border-red-500/50 rounded-lg">
            <p class="text-red-300 font-semibold">✗ Something went wrong</p>
            <p class="text-red-400 text-sm mt-1">Please try again or contact us directly.</p>
          </div>
        </div>

        <!-- Contact Info & Benefits -->
        <div>
          <div class="card border-l-4 border-cyan-500 mb-8">
            <h3 class="text-2xl font-bold mb-6">Why Contact Us?</h3>
            <ul class="space-y-4">
              <li class="flex items-start">
                <span class="text-2xl mr-3 text-green-400">✓</span>
                <div>
                  <strong>Free AI Readiness Assessment</strong>
                  <p class="text-sm text-gray-400">Understand your AI opportunities and potential ROI</p>
                </div>
              </li>
              <li class="flex items-start">
                <span class="text-2xl mr-3 text-green-400">✓</span>
                <div>
                  <strong>Expert Consultation</strong>
                  <p class="text-sm text-gray-400">Speak directly with our founders about your needs</p>
                </div>
              </li>
              <li class="flex items-start">
                <span class="text-2xl mr-3 text-green-400">✓</span>
                <div>
                  <strong>Custom Roadmap</strong>
                  <p class="text-sm text-gray-400">Get a personalized AI implementation plan</p>
                </div>
              </li>
              <li class="flex items-start">
                <span class="text-2xl mr-3 text-green-400">✓</span>
                <div>
                  <strong>Transparent Pricing</strong>
                  <p class="text-sm text-gray-400">Clear costs and ROI projections upfront</p>
                </div>
              </li>
              <li class="flex items-start">
                <span class="text-2xl mr-3 text-green-400">✓</span>
                <div>
                  <strong>No Commitment</strong>
                  <p class="text-sm text-gray-400">Free consultation with no obligation</p>
                </div>
              </li>
            </ul>
          </div>

          <!-- Contact Information -->
          <div class="card">
            <h3 class="text-2xl font-bold mb-6">Get in Touch</h3>
            <div class="space-y-6">
              {contactInfo.map((info) => (
                <div class="flex items-start">
                  <span class="text-3xl mr-4">{info.icon}</span>
                  <div>
                    <h4 class="font-semibold mb-1">{info.title}</h4>
                    <a
                      href={info.link}
                      class="text-cyan-400 hover:text-cyan-300 transition-colors"
                      target={info.link.startsWith('http') ? '_blank' : undefined}
                      rel={info.link.startsWith('http') ? 'noopener noreferrer' : undefined}
                    >
                      {info.value}
                    </a>
                  </div>
                </div>
              ))}
            </div>

            <div class="mt-8 pt-8 border-t border-cyan-500/20">
              <h4 class="font-semibold mb-4">Follow Us</h4>
              <div class="flex space-x-4">
                <a
                  href="https://www.linkedin.com/company/drag-ai/"
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-gray-400 hover:text-cyan-400 transition-colors"
                  aria-label="LinkedIn"
                >
                  <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
                  </svg>
                </a>
                <a
                  href="https://x.com/drag_ai"
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-gray-400 hover:text-cyan-400 transition-colors"
                  aria-label="Twitter"
                >
                  <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
                  </svg>
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="section bg-slate-900/30">
    <div class="container">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl font-bold text-center mb-12">Frequently Asked Questions</h2>
        
        <div class="space-y-6">
          <div class="card">
            <h3 class="text-xl font-bold mb-2">How quickly can we get started?</h3>
            <p>
              We can schedule your free AI assessment within 48 hours. After that, pilot projects typically start within 2-3 weeks.
            </p>
          </div>

          <div class="card">
            <h3 class="text-xl font-bold mb-2">What does the free assessment include?</h3>
            <p>
              A 60-90 minute consultation where we analyze your operations, identify AI opportunities, and provide a custom roadmap with ROI projections.
            </p>
          </div>

          <div class="card">
            <h3 class="text-xl font-bold mb-2">Do we need technical expertise?</h3>
            <p>
              No! That's the whole point. We handle all the technical complexity. You just need to understand your business challenges.
            </p>
          </div>

          <div class="card">
            <h3 class="text-xl font-bold mb-2">What's the typical investment?</h3>
            <p>
              Pilot projects range from $25K-$75K. Full implementations typically $100K-$500K depending on scope. We provide detailed pricing upfront.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<script>
  // Form submission handling
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const successMessage = document.getElementById('form-success');
  const errorMessage = document.getElementById('form-error');

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();

    const formData = new FormData(form);
    const submitButton = form.querySelector('button[type="submit"]') as HTMLButtonElement;
    
    // Disable button and show loading state
    if (submitButton) {
      submitButton.disabled = true;
      submitButton.textContent = 'Sending...';
    }

    try {
      const response = await fetch('https://api.web3forms.com/submit', {
        method: 'POST',
        body: formData
      });

      if (response.ok) {
        // Show success message
        successMessage?.classList.remove('hidden');
        errorMessage?.classList.add('hidden');
        form.reset();
        
        // Scroll to success message
        successMessage?.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
      } else {
        throw new Error('Form submission failed');
      }
    } catch (error) {
      // Show error message
      errorMessage?.classList.remove('hidden');
      successMessage?.classList.add('hidden');
      
      // Scroll to error message
      errorMessage?.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    } finally {
      // Re-enable button
      if (submitButton) {
        submitButton.disabled = false;
        submitButton.textContent = 'Get Free Assessment';
      }
    }
  });
</script>